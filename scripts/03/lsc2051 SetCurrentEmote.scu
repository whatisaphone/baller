; LSC2 3 2051 SetCurrentEmote

parameter emote: Emote

rCurrentEmote = emote
if (rCurrentEmote) {
    sleep 20
    if (rCurrentEmote == 9) {
        sleep 40
    }
    rCurrentEmote = 0
}
free-script

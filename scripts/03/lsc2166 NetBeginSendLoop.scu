; LSC2 3 2166 NetBeginSendLoop

if (gNetplayActive) {
    if (!gNetStateBuffer) {
        dim array gNetStateBuffer i16[0...21][0...5] swap=2
    }
    if (g_net_smth_me == g_net_smth_master) {
        begin-script NetSendLoop 3 []
    }
}
free-script

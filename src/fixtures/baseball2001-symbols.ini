; test comments

; and blank lines

global.1 = the_first_global

script.1 = script_one

; 1. test sparsely naming locals
; 2. script 1 doesn't actually use local 0, so test a local it actually uses
script.1.local.2 = window_title

room.1.script.2049 = local_script

room.1.script.2049.local.0 = local_script_var

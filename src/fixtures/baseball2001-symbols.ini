; test comments

; and blank lines

global.1 = the_first_global ; and comments after entries
global.29 = script_var: Script

script.1 = script_one

; 1. test sparsely naming locals
; 2. script 1 doesn't actually use local 0, so test a local it actually uses
script.1.local.2 = window_title

room.1.script.2049 = local_script

room.1.script.2049.local.0 = local_script_var

room.2.var.1 = a_room_var

room.3.var.0 = room_var_used_in_encd

room.5.enter.local.0 = encd_var
room.5.exit.local.0 = excd_var

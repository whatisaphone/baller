; test comments

; and blank lines

global.1 = the_first_global ; and comments after entries
global.29 = script_var: Script

; in script 1, this is set to 0. since there is no script 0, writing `scr0`
; would cause an error when recompiling. instead, make sure it falls back to a
; literal `0`.
global.38 = script_var_zero: Script

; script 1 doesn't actually use local 0, so test a local it actually uses
script.1 = script_one(_) _ window_title

room.1.script.2049 = local_script() local_script_var

room.2.var.1 = a_room_var

room.3.var.0 = room_var_used_in_encd

room.5.enter = () encd_var
room.5.exit = () excd_var

; test sparse enums
enum.MachineSpeed.0 = SLOW
enum.MachineSpeed.2 = FAST
global.215 = machine-speed: MachineSpeed
